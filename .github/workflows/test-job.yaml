name: Test

on:
  push:
    branches: [develop, main]

jobs:
  patch:
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          caches: yarn

      - name: Echo node
        run: yarn --version